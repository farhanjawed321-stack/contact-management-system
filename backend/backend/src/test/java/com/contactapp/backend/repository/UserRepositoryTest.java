package com.contactapp.backend.repository;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

// Disabled: requires JPA context and database. Skipping in lightweight unit runs.
@Disabled("Integration test - requires database")
class UserRepositoryTest {

    @Test
    void placeholder() {
        // Intentionally left blank; real repository tests require a DB.
    }
}